# Hack to remove weird sting of characters on startup
set -sg escape-time 50

# Mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Set true colors
set-option -g focus-events on
set -sg terminal-overrides ",*:RGB"

# Set prefix to ctrl + space
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Bind r to source this file
unbind r
bind r source ~/.config/tmux/tmux.conf

# Shift Alt vim keys to switch windows
bind -n M-h previous-window
bind -n M-l next-window

# forget the find window.  That is for chumps
bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"

# Bind intuitive window splits
bind | split-window -h
bind _ split-window -v

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

# Set vi-mode
#set-window-option -g mode-keys vi

# ^ Keybindings
# bind-key -T copy-mode-vi v send-keys -X begin-selection
# bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
# bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Open panes in the current directory
bind '_' split-window -v -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"

# Make sure your 16-color terminal theme uses slightly
# different colors for `background` vs `black` (AKA `color0`).
# "Nord" or "Tango dark" themes do this out of the box.
# Use `white` instead of `black` for light themes.


# Status bar customization
# set -g status-interval 3     # update the status bar every 3 seconds
# set -g status-left "#[fg=#282828,bg=default]  #S #[fg=#282828,bg=default]|"
# set -g status-right "#[fg=#282828,bg=default] "
# set -g status-justify left
# set -g status-left-length 200    # increase length (from 10)
# set -g status-position bottom
# set -g status-style 'bg=#9c9c9c' # transparent
# set -g window-status-current-format '#[fg=#282828,bg=default] #I #W'
# set -g window-status-format '#[fg=gray,bg=default] #I #W'
# set -g window-status-last-style 'fg=white,bg=default'
# set -g default-terminal "${TERM}"
# set -g message-command-style bg=default,fg=yellow
# set -g message-style bg=default,fg=#282828
# set -g mode-style bg=default,fg=yellow

set -g @tmux-last-color on

run '~/.tmux/plugins/tpm/tpm'
